import React from "react";
import { useCharacters, useLocation } from "../api/useData";
import { useFetch } from "../api/useFetch";


export default function Main(characters){
    // console.log(characters);
    return(
        <div className="infos">
           {/* {characters ? characters.characters.results.map( (a)=>(<div>{a.name}</div>)): <div>{"valami"}</div> */}
            {/* } */}
            
            

        </div>
    )
}
